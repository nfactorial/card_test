'use strict';

var chai = require('chai');
var expect = chai.expect;
/*
var GameController = require('../../../development/scripts/game_controller/game_controller.js');

describe('client/GameController', function() {
    it('Should be empty after construction.', function() {
        var item = new GameController();

        expect(item.server).to.equal(null);
    });

    it('Should allow subscription to events.', function() {
        var game = GameController();

        //expect(game.subscribe())
    });
});
*/